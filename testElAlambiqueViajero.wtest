import elAlambiqueViajero.*
import recuerdoDeProvincia.*
describe "Grupo de purebas"{
    test "el superconvertible se transforma en una moto y luke se sube a el"{
        superConvertible.convertir(moto)
        luke.vehiculo(superConvertible)
        assert.that(luke.vehiculo())
    }
}